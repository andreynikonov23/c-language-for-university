#include <stdio.h>

//Записать инструкцию, которая выводит в одной строке значения переменных а, b и с целого типа (int).

int main(){
    //Инициализация переменных
    int a = 10;
    int b = 20;
    int c = 30;

    //Вывод
    printf("a = %d; b = %d; c = %d", a, b, c);
    return 0;
}